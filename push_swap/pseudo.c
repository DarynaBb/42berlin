//Declare pointers to two data structures/linkd lists, one for stack 'a' and another for stack 'b'
	//Set both pointers to NULL

//Handle input count errors. Argument count must be 2 or more, and the second input must be not empty
	//If input errors, return error

//Handle both cases of input, whether a variable number of comand line arguments, or as a string
	//If the input of number is a string, call `split()` to split the substrings

//Initialize stack `a` by apending each input number as a node to stack `a`
	//Handle integer overflow, duplicates, and syntax errors, e.g. input must only contain digits, or `-` `+` signs
		//If errors found, free stack `a` and return error
	//Check for each input if it is a lonf integer
		//If the input is a string, convert it to a long integer
	//Append the nodes to stack `a`

//Check if stack a is sorted
	//If its not sorted, implement our sorting algorithm
		//Check for two numbers
			//If so, simply swap th numbers
		//Check for three numbers
			//If so, implement our simple `sort three` algorithm
		//Check if the stack has more than 3 numbers
			//If so, implement our Turk algorithm


//Basic utils
	//Functions to handle errors
	//Operations
		//sa
		//sb
		//ss
		//pa
		//pb
		//ra
		//rb
		//rr
		//rra
		//rrb
		//rra
	//Stack length
	//Last node
	//Min and max nodes	


	//check if n_param < 2 - return (0);
	//if n >= 2 - if it is a valid int (INT_MIN/INT_MAX);
